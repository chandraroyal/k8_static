apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
 name: static-ingress
 namespace: static
 annotations:
   kubernetes.io/ingress.class: nginx
spec:
 rules:
 - host: dev-static.faizepro.xyz
   http:
     paths:
     - path:
       backend:
         serviceName: k8s-static-svc
         servicePort: 80
